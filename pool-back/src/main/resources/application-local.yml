spring:
  jpa:
    show-sql: true

  # Bổ sung thêm thư mục seed-local cho Flyway (chứa V2, V3 demo data)
  flyway:
    locations:
      - classpath:db/migration
      - classpath:db/seed-local

# Cấu hình mức độ log để dễ debug Security
logging:
  level:
    org.springframework.security: DEBUG